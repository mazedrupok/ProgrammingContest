#include <bits/stdc++.h>
using namespace std;
int main()
{
    int tc,a,b, k = 0, c;
    //cout << 2%1 << endl;
    scanf (" %d", &tc);
    while (tc--) {
        scanf (" %d %d", &a, &b);
        if (b == 1) printf ("No\n");
        else printf ("Yes\n");
    }
    return 0;
}
